import add from '@src/math/add'

describe('this is a test', () =>{
    it('should pass', () => {
        expect(add(1,2)).toBe(7);
    });
});

describe('this is a test', () =>{
    it('should pass', () => {
        expect(add(1,2)).toBe(3);
    });
});
